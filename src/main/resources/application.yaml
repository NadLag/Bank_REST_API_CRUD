spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bank_management
    username: root
    password: pqpq
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true
  application:
    name: BankManagement